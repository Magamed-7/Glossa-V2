# Django auth_service — register, login, refresh, /me, 2FA, password change
VITE_AUTH_URL=http://127.0.0.1:8001

# FastAPI app — everything else (deck, stories, grammar, social, payments, ...)
VITE_API_URL=http://127.0.0.1:8000

# FastAPI websocket_app — AI chat
VITE_WS_URL=ws://127.0.0.1:8002
