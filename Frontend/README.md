# Glossa Frontend

Not built yet. This folder currently holds design-direction mockups (`design/`) and the design system notes
(`Plan/DESIGN_SYSTEM.md`) from an earlier exploration phase, not a real application.

When frontend work starts, the backend integration contract — base URLs, auth flow, error format, pagination,
locales, the WebSocket AI chat protocol, and the full endpoint list — is at
[`../docs/FRONTEND_CONTRACT.md`](../docs/FRONTEND_CONTRACT.md). Backend setup/run instructions are at
[`../Backend/README.md`](../Backend/README.md).
