# Standard cumulative CEFR word-family size benchmarks, used both by the
# vocabulary-size test scoring and as a PDF-export fallback when the user
# hasn't taken (or confirmed) that test yet.
CEFR_VOCAB_BENCHMARK = {
    'A1': 500,
    'A2': 1000,
    'B1': 2000,
    'B2': 4000,
    'C1': 8000,
    'C2': 16000,
}
