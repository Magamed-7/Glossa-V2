# Glossa auth_service

Django + DRF + SimpleJWT service: registration, login/refresh, `/me`, admin.

Create a superuser (from `Backend/auth_service`, with `Backend/.venv` active):

```
python manage.py createsuperuser
```

Then open `/admin/` and log in with those credentials.
